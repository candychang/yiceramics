angular.module('works', [])
.controller('GalleryCtrl', ['$scope', 'galleryService',
    function($scope, $galleryService) {
        $scope.playful_images = [];
    
        $scope.serious_images = [];
        
        $galleryService.getGallery()
        .then(function(data) {
            // promise fulfilled
            angular.copy(data.playful, $scope.playful_images);
            angular.copy(data.serious, $scope.serious_images);
        }, function(error) {
            console.log('error', error);
        });
    
        
    
    $scope.methods = {};
    
    $scope.openGallery = function(){
    
        $scope.methods.open(index); 
    };
    
    $scope.closeGallery = function(){
        $scope.methods.close();
    };
    
    $scope.nextImg = function(){
        $scope.methods.next();
    };
    
    $scope.prevImg = function(){
        $scope.methods.prev();
    };
    }]);